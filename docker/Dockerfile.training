FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml ./
COPY src ./src
RUN pip install --no-cache-dir .

ENTRYPOINT ["log-analyzer"]
CMD ["train", "--model", "isolation_forest", "--n-train", "10000"]
